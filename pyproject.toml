[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "coronagraph"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "matplotlib",
]
